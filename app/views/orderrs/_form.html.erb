<%= form_with(model: orderr, local: true) do |form| %>
  <% if orderr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(orderr.errors.count, "error") %> prohibited this orderr from being saved:</h2>

      <ul>
      <% orderr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :shipping_id %>
    <%= form.text_field :shipping_id %>
  </div> 

  <div class="field">
    <%= form.label :billing_id %>
    <%= form.text_field :billing_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
